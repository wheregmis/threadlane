[package]
name = "broker-smoke-ext"
version = "0.1.0"
edition = "2021"
description = "WASI capability broker smoke extension"

[lib]
crate-type = ["cdylib"]

[features]
agent-only = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
