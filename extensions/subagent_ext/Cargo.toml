[package]
name = "subagent-ext"
version = "0.1.0"
edition = "2021"
description = "Subagent WASI extension for threadlane"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
